{% extends 'base.html.twig' %}

{% block title %}PFEController!{% endblock %}

{% block body %}

<div >
    <div class="card text-white bg-primary mb-3">
        <div class="card-header">
            <h3>{{ pfe.title }}</h3>
        </div>
        <div class="card-body">
            <h5 class="card-title">Student: {{ pfe.student }}</h5>
            <p class="card-text">Etp. :{{ pfe.entreprise }}</p>

    </div>
</div>
{% endblock %}
